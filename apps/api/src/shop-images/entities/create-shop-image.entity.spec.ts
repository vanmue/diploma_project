import { CreateShopImageEntity } from './create-shop-image.entity';

describe('CreateShopImageEntity', () => {
  it('should be defined', () => {
    expect(new CreateShopImageEntity()).toBeDefined();
  });
});
