import { MasterWeekendEntity } from './master-weekend.entity';

describe('MasterWeekendEntity', () => {
  it('should be defined', () => {
    expect(new MasterWeekendEntity()).toBeDefined();
  });
});
