import { CreateMasterWeekendEntity } from './create-master-weekend.entity';

describe('CreateMasterWeekendEntity', () => {
  it('should be defined', () => {
    expect(new CreateMasterWeekendEntity()).toBeDefined();
  });
});
