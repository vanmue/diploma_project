import { ListAllAppointmentsDto } from './list-all-appointments.dto';

describe('ListAllAppointmentsDto', () => {
  it('should be defined', () => {
    expect(new ListAllAppointmentsDto()).toBeDefined();
  });
});
