import { CreateReviewEntity } from './create-review.entity';

describe('CreateMasterReviewEntity', () => {
  it('should be defined', () => {
    expect(new CreateReviewEntity()).toBeDefined();
  });
});
