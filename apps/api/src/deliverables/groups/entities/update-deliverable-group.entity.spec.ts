import { UpdateDeliverableGroupEntity } from './update-deliverable-group.entity';

describe('UpdateDeliverableGroupEntity', () => {
  it('should be defined', () => {
    expect(new UpdateDeliverableGroupEntity()).toBeDefined();
  });
});
