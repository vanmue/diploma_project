import { DeliverableGroupEntity } from './deliverable-group.entity';

describe('GroupEntity', () => {
  it('should be defined', () => {
    expect(new DeliverableGroupEntity()).toBeDefined();
  });
});
