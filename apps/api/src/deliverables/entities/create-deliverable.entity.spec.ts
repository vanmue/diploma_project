import { CreateDeliverableEntity } from './create-deliverable.entity';

describe('CreateDeliverableEntity', () => {
  it('should be defined', () => {
    expect(new CreateDeliverableEntity()).toBeDefined();
  });
});
