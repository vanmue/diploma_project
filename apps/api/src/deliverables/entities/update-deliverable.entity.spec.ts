import { UpdateDeliverableEntity } from './update-deliverable.entity';

describe('UpdateDeliverableEntity', () => {
  it('should be defined', () => {
    expect(new UpdateDeliverableEntity()).toBeDefined();
  });
});
