import { ShopAdvantageEntity } from './shop-advantage.entity';

describe('ShopAdvantagesEntity', () => {
  it('should be defined', () => {
    expect(new ShopAdvantageEntity()).toBeDefined();
  });
});
