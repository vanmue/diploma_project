import { UpdateShopAdvantageEntity } from './update-shop-advantage.entity';

describe('UpdateShopAdvantageEntity', () => {
  it('should be defined', () => {
    expect(new UpdateShopAdvantageEntity()).toBeDefined();
  });
});
