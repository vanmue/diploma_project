import { CreateProfileEntity } from './create-profile.entity';

describe('CreateProfileEntity', () => {
  it('should be defined', () => {
    expect(new CreateProfileEntity()).toBeDefined();
  });
});
