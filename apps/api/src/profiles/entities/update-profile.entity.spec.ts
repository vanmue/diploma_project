import { UpdateProfileEntity } from './update-profile.entity';

describe('UpdateProfileEntity', () => {
  it('should be defined', () => {
    expect(new UpdateProfileEntity()).toBeDefined();
  });
});
